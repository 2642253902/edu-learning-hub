<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.exampe.study.mapper.ResourceReviewMapper">

    <select id="listByResourceId" resultType="com.exampe.study.entity.ResourceReview">
        SELECT id, resource_id, course_id, user_id, username, rating, content, likes, create_time
        FROM resource_review
        WHERE resource_id = #{resourceId}
        ORDER BY create_time DESC
    </select>

    <update id="incrementLike">
        UPDATE resource_review
        SET likes = IFNULL(likes, 0) + 1
        WHERE id = #{id}
    </update>

</mapper>
