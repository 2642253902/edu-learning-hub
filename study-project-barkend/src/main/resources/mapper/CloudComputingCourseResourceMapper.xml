<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.exampe.study.mapper.CloudComputingCourseResourceMapper">

    <!-- 查询课程资源列表，并关联学生的学习状态记录 -->
    <select id="listWithLearningStatus" resultType="com.exampe.study.dto.CloudComputingCourseResourceVO">
        SELECT r.id,
               r.create_by        AS createBy,
               r.create_time      AS createTime,
               r.update_by        AS updateBy,
               r.update_time      AS updateTime,
               r.resource_type    AS resourceType,
               r.course_id        AS courseId,
               r.resource_name    AS resourceName,
               r.resource_url     AS resourceUrl,
               r.resource_sort    AS resourceSort,
               lr.id              AS recordId,
               lr.user_id         AS studentId,
               lr.content_id      AS contentId,
               lr.learning_status AS learningStatus,
               lr.learning_time   AS learningTime,
               lr.last_learn_time AS lastLearnTime,
               lr.create_time     AS recordCreateTime
        FROM cloud_computing_course_resource r
                 LEFT JOIN cloud_computing_student_learning_record lr
                           ON r.id = lr.content_id
                               AND (#{studentId} IS NULL OR lr.user_id = #{studentId})
                               AND lr.course_id = #{courseId}
        WHERE r.course_id = #{courseId}
        ORDER BY r.resource_sort ASC
    </select>

</mapper>
