# 开发环境配置文件，供后端本地运行和前端联调时使用。
spring:
  # 数据源配置
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/study?useUnicode=true&characterEncoding=UTF-8
    username: root
    password: root
  
  # 邮件服务配置
  mail:
    host: smtp.163.com
    username: 18897793826@163.com
    password: BSX8mPRLBC3RZ6vs
    properties:
      from: 18897793826@163.com
  
  # Redis 缓存配置
  data:
    redis:
      database: 0
      port: 6379
      host: localhost
      password: ""
  
  # 文件上传配置
  servlet:
    multipart:
      # 单个文件最大大小
      max-file-size: 500MB
      # 整个请求最大大小
      max-request-size: 500MB
      # 临时文件存储路径（Windows 路径示例）
      location: D:/Desktop/opt
  ai:
    openai:
      api-key: sk-1241ce0548c74552b2a4934c02f9f126
      base-url: https://api.deepseek.com
      chat:
        options:
          model: deepseek-chat

# MyBatis 配置
mybatis:
  mapper-locations: classpath:mapper/**/*.xml

# MyBatis-Plus 配置
mybatis-plus:
  mapper-locations: classpath:mapper/**/*.xml
