# 生产环境配置文件，供后端部署时与前端静态资源一并联调。
spring:
  # 数据源配置（生产环境需要修改为实际的生产数据库配置）
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://111.228.53.34:3306/study?useUnicode=true&characterEncoding=UTF-8
    username: root
    password: 123456Ok!

  # 邮件服务配置（生产环境建议使用企业邮箱）
  mail:
    host: smtp.163.com
    port: 465
    username: 18897793826@163.com
    password: BSX8mPRLBC3RZ6vs
    properties:
      mail:
        smtp:
          auth: true
          ssl:
            enable: true
          connectiontimeout: 10000   # 毫秒，建立连接超时 10s
          timeout: 10000             # 毫秒，读取响应超时 10s
          writetimeout: 10000        # 毫秒，写数据超时 10s

  # Redis 缓存配置（生产环境需要修改为实际的 Redis 服务器配置）
  data:
    redis:
      database: 0
      port: 6379
      host: 111.228.53.34
      password: '123456Ok!'

  # 文件上传配置
  servlet:
    multipart:
      # 单个文件最大大小
      max-file-size: 500MB
      # 整个请求最大大小
      max-request-size: 500MB
      # 临时文件存储路径（生产环境建议使用 Linux 路径）
      location: /opt/upFiles

  # AI 服务配置
  ai:
    openai:
      # API 密钥，用于访问 AI 服务提供商的接口
      api-key: sk-1241ce0548c74552b2a4934c02f9f126
      # AI 服务的 Base URL，当前配置为 DeepSeek 的 API 地址
      base-url: https://api.deepseek.com
      chat:
        options:
          # 使用的 AI 模型名称，当前配置为 deepseek-chat
          model: deepseek-chat

# MyBatis 配置
mybatis:
  mapper-locations: classpath:mapper/**/*.xml

# MyBatis-Plus 配置
mybatis-plus:
  mapper-locations: classpath:mapper/**/*.xml
